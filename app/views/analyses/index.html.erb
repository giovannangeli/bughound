<div class="analyses-container">
  <div class="analyses-header">
    <h1 class="analyses-title">Historique des analyses</h1>
    <p class="analyses-subtitle">Retrouvez toutes vos analyses de code précédentes</p>
  </div>

  <div class="analyses-card">
    <% if @languages.present? %>
      <div class="filters-section">
        <h3 class="filters-title">Filtres</h3>
        <div class="filters-grid">
          <div class="filter-group">
            <label class="filter-label">Langage</label>
            <div class="select-wrapper">
              <select class="language-select" onchange="updateFilters('language', this.value)">
                <option value="">Tous les langages</option>
                <% @languages.each do |lang| %>
                  <option value="<%= lang %>" <%= 'selected' if params[:language] == lang %>><%= lang %></option>
                <% end %>
              </select>
              <svg class="select-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </div>
          </div>
          
          <div class="filter-group">
            <label class="filter-label">Type d'analyse</label>
            <div class="select-wrapper">
              <select class="language-select" onchange="updateFilters('ai_provider', this.value)">
                <option value="">Tous les types</option>
                <option value="openai" <%= 'selected' if params[:ai_provider] == 'openai' %>>Analyse rapide</option>
                <option value="claude" <%= 'selected' if params[:ai_provider] == 'claude' %>>Analyse expert</option>
                <option value="tests" <%= 'selected' if params[:ai_provider] == 'tests' %>>Tests automatiques</option>
                <option value="smells" <%= 'selected' if params[:ai_provider] == 'smells' %>>Code smells</option>
                <option value="improve" <%= 'selected' if params[:ai_provider] == 'improve' %>>Refactoring</option>
              </select>
              <svg class="select-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <div class="content-section">
      <% if @analyses.empty? %>
        <div class="empty-state">
          <div class="empty-icon">🔍</div>
          <h3 class="empty-title">Aucune analyse trouvée</h3>
          <p class="empty-description">
            <% if params[:language].present? %>
              Aucune analyse en <%= params[:language] %> pour le moment.
            <% else %>
              Commencez par créer votre première analyse de code !
            <% end %>
          </p>
          <%= link_to new_analysis_path, class: "btn-primary" do %>
            <i class="fas fa-plus"></i>
            <span>Nouvelle analyse</span>
          <% end %>
        </div>
      <% else %>
        <div class="table-container">
          <table class="analyses-table">
            <thead>
              <tr>
                <th>Titre</th>
                <th>Type</th>
                <th>Langage</th>
                <th>Score</th>
                <th>Date</th>
                <th>Action</th>
              </tr>
            </thead>
            <tbody>
              <% @analyses.each do |analysis| %>
                <tr class="table-row">
                  <td class="title-cell">
                    <div class="title-content">
                      <%= analysis.title.present? ? analysis.title : "Analyse sans titre" %>
                    </div>
                  </td>
                  <td class="type-cell">
                    <% type_info = analysis_type_info(analysis.ai_provider) %>
                    <span class="type-badge" style="background-color: <%= type_info[:color] %>15; color: <%= type_info[:color] %>; border: 1px solid <%= type_info[:color] %>30;">
                      <i class="fas <%= type_info[:icon] %>"></i>
                      <%= type_info[:name] %>
                    </span>
                  </td>
                  <td class="language-cell">
                    <span class="language-badge"><%= analysis.language %></span>
                  </td>
                  <td class="score-cell">
                    <% score = extract_global_score(analysis.ai_feedback, analysis.ai_provider) %>
                    <% badge_info = score_badge_info(score) %>
                    <span class="score-badge" style="background-color: <%= badge_info[:bg_color] %>; color: <%= badge_info[:color] %>; border: 1px solid <%= badge_info[:color] %>30;">
                      <i class="fas fa-chart-line"></i>
                      <%= badge_info[:text] %>
                    </span>
                  </td>
                  <td class="date-cell">
                    <%= l(analysis.created_at, format: :short) %>
                  </td>
                  <td class="action-cell">
                    <div class="dropdown-container" data-dropdown>
                      <button class="btn-dropdown" data-dropdown-trigger>
                        <i class="fas fa-eye"></i>
                        <span>Voir</span>
                        <i class="fas fa-chevron-down dropdown-chevron"></i>
                      </button>
                      <div class="dropdown-menu" data-dropdown-menu>
                        <%= link_to analysis_path(analysis), class: "dropdown-item dropdown-item-primary" do %>
                          <i class="fas fa-eye"></i>
                          <span>Voir l'analyse</span>
                        <% end %>
<%= link_to duplicate_analysis_path(analysis), method: :post, class: "dropdown-item", data: { turbo_method: :post } do %>
  <i class="fas fa-copy"></i>
  <span>Dupliquer</span>
<% end %>
<%= link_to analysis_path(analysis), method: :delete, 
    class: "dropdown-item dropdown-item-danger",
    data: { 
      turbo_method: :delete,
      turbo_confirm: "🗑️ Êtes-vous sûr de vouloir supprimer '#{analysis.title.present? ? analysis.title : "Analyse sans titre"}' ?\n\nCette action est irréversible."
    } do %>
  <i class="fas fa-trash"></i>
  <span>Supprimer</span>
<% end %>
                      </div>
                    </div>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>

        <div class="bottom-actions">
          <%= link_to new_analysis_path, class: "btn-primary" do %>
            <i class="fas fa-plus"></i>
            <span>Nouvelle analyse</span>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>

<style>
/* Messages d'alerte */
.alert {
  padding: 1rem 1.5rem;
  margin: 1rem;
  border-radius: 8px;
  font-weight: 500;
  position: relative;
  animation: slideDown 0.3s ease;
  /* AUTO-DISPARITION FLUIDE */
  animation: slideDown 0.3s ease, slideUp 0.5s ease 3s forwards;
}

@keyframes slideUp {
  0% { 
    opacity: 1; 
    max-height: 100px; 
    margin: 1rem; 
    padding: 1rem 1.5rem; 
  }
  100% { 
    opacity: 0; 
    max-height: 0; 
    margin: 0; 
    padding: 0 1.5rem; 
  }
}
.alert-success {
  background: #10b981;
  color: white;
  border: 1px solid #059669;
}

.alert-danger {
  background: #ef4444;
  color: white;
  border: 1px solid #dc2626;
}

@keyframes slideDown {
  from { transform: translateY(-20px); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}
* { margin: 0; padding: 0; box-sizing: border-box; }
body { 
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Inter', sans-serif; 
  background: #fafafa; 
  color: #0a0a0a; 
  line-height: 1.6; 
  -webkit-font-smoothing: antialiased; 
  -moz-osx-font-smoothing: grayscale; 
}

.analyses-container { max-width: 1000px; margin: 0 auto; padding: 3rem 1.5rem; }

/* Header élégant */
.analyses-header { text-align: center; margin-bottom: 3rem; }
.analyses-title { 
  font-size: 2.5rem; 
  font-weight: 700; 
  color: #0a0a0a; 
  margin-bottom: 0.75rem; 
  letter-spacing: -0.02em; 
}
.analyses-subtitle { 
  font-size: 1.125rem; 
  color: #6b7280; 
  font-weight: 400; 
}

/* Card principale */
.analyses-card { 
  background: white; 
  border-radius: 20px; 
  padding: 2.5rem; 
  box-shadow: 0 4px 25px rgba(0,0,0,0.08); 
  border: 1px solid #e5e7eb; 
}

/* Section des filtres */
.filters-section { margin-bottom: 2.5rem; }
.filters-title { 
  font-size: 1rem; 
  font-weight: 600; 
  color: #1f2937; 
  margin-bottom: 1.25rem; 
}

/* Grid de filtres */
.filters-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.5rem;
}

.filter-group {
  display: flex;
  flex-direction: column;
}

.filter-label {
  font-size: 0.875rem;
  font-weight: 500;
  color: #374151;
  margin-bottom: 0.5rem;
}

/* Dropdown moderne */
.select-wrapper { 
  position: relative; 
}

.language-select {
  width: 100%; 
  padding: 0.875rem 1.125rem; 
  padding-right: 2.75rem;
  font-size: 0.9375rem; 
  border: 1.5px solid #e5e7eb; 
  border-radius: 12px; 
  background: white; 
  color: #0a0a0a; 
  transition: all .2s ease;
  appearance: none;
  cursor: pointer;
}

.language-select:focus { 
  outline: none; 
  border-color: #3b82f6; 
  box-shadow: 0 0 0 4px rgba(59,130,246,.08); 
}

.select-icon { 
  position: absolute; 
  right: 1.125rem; 
  top: 50%; 
  transform: translateY(-50%); 
  width: 20px; 
  height: 20px; 
  color: #6b7280; 
  pointer-events: none; 
}

/* Empty state moderne */
.empty-state {
  text-align: center;
  padding: 4rem 2rem;
}
.empty-icon {
  font-size: 4rem;
  margin-bottom: 1.5rem;
  opacity: 0.6;
}
.empty-title {
  font-size: 1.5rem;
  font-weight: 600;
  color: #1f2937;
  margin-bottom: 0.75rem;
}
.empty-description {
  font-size: 1rem;
  color: #6b7280;
  margin-bottom: 2rem;
  max-width: 400px;
  margin-left: auto;
  margin-right: auto;
}

/* Tableau moderne */
.table-container {
  border-radius: 12px;
  border: 1px solid #e5e7eb;
}

.analyses-table {
  width: 100%;
  border-collapse: collapse;
  background: white;
  table-layout: fixed;
}

/* Headers avec alignement parfait */
.analyses-table th {
  background: #f8fafc;
  padding: 1rem 1.5rem;
  text-align: left;
  font-weight: 600;
  font-size: 0.875rem;
  color: #374151;
  border-bottom: 1px solid #e5e7eb;
}

/* Alignement spécifique des headers */
.analyses-table th:nth-child(2), /* Type */
.analyses-table th:nth-child(3), /* Langage */
.analyses-table th:nth-child(4), /* Score */
.analyses-table th:nth-child(6) { /* Action */
  text-align: center;
}


.table-row {
  transition: all 0.2s ease;
}

.table-row:hover {
  background: #f8fafc;
}

.table-row td {
  padding: 1rem 1.5rem;
  border-bottom: 1px solid #f1f5f9;
  vertical-align: middle;
}

.title-cell {
  font-weight: 500;
  color: #1f2937;
}

/* Alignement des cellules avec !important pour forcer */
.type-cell, .language-cell, .score-cell, .action-cell {
  text-align: center !important;
}

.type-badge, .score-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.375rem;
  padding: 0.25rem 0.75rem;
  border-radius: 20px;
  font-size: 0.8125rem;
  font-weight: 500;
  white-space: nowrap;
}

.type-badge i, .score-badge i {
  font-size: 0.75rem;
}

.language-badge {
  display: inline-block;
  padding: 0.25rem 0.75rem;
  background: #eff6ff;
  color: #1d4ed8;
  border-radius: 20px;
  font-size: 0.8125rem;
  font-weight: 500;
}

.date-cell {
  color: #6b7280;
  font-size: 0.875rem;
}

/* Boutons modernes */
.btn-primary {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.75rem 1.5rem;
  background: linear-gradient(135deg, #10b981, #059669);
  color: white;
  text-decoration: none;
  border-radius: 10px;
  font-weight: 600;
  font-size: 0.9375rem;
  transition: all 0.2s ease;
  box-shadow: 0 2px 8px rgba(16, 185, 129, 0.2);
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 20px rgba(16, 185, 129, 0.3);
}

/* Dropdown Container */
.dropdown-container {
  position: relative;
  display: inline-block;
}

/* Dropdown Trigger Button - Version compacte */
.btn-dropdown {
  display: inline-flex;
  align-items: center;
  gap: 0.25rem;
  padding: 0.5rem 0.75rem;
  background: transparent;
  color: #64748b;
  text-decoration: none;
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  font-weight: 500;
  font-size: 0.8125rem;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
  cursor: pointer;
}

.btn-dropdown::before {
  content: '';
  position: absolute;
  inset: 0;
  padding: 1px;
  background: linear-gradient(45deg, #ff6b6b, #4ecdc4, #45b7d1, #96ceb4, #ffeaa7, #dda0dd, #98d8c8);
  background-size: 400% 400%;
  border-radius: 8px;
  opacity: 0;
  transition: all .3s ease;
  animation: gradientShift 3s ease infinite;
  mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  mask-composite: xor;
  -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
}

.btn-dropdown:hover::before {
  opacity: 1;
}

.btn-dropdown:hover {
  background: white;
  color: #1e293b;
  border-color: transparent;
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(0,0,0,.1);
}

.dropdown-chevron {
  font-size: 0.625rem;
  margin-left: 0.125rem;
  transition: transform 0.2s ease;
}

.dropdown-container.open .dropdown-chevron {
  transform: rotate(180deg);
}

/* Dropdown Menu - Version compacte */
.dropdown-menu {
  position: absolute;
  top: 100%;
  right: 0;
  min-width: 150px;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 12px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.12), 0 2px 8px rgba(0, 0, 0, 0.08);
  padding: 0.375rem 0;
  z-index: 1000;
  opacity: 0;
  visibility: hidden;
  transform: translateY(-8px) scale(0.95);
  transition: all 0.2s ease;
}

.dropdown-container.open .dropdown-menu {
  opacity: 1;
  visibility: visible;
  transform: translateY(0) scale(1);
}

/* Dropdown Items - Version compacte */
.dropdown-item {
  display: flex;
  align-items: center;
  gap: 0.625rem;
  width: 100%;
  padding: 0.625rem 0.875rem;
  color: #374151;
  text-decoration: none;
  background: none;
  border: none;
  font-size: 0.8125rem;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
  position: relative;
}

.dropdown-item:hover {
  background: rgba(59, 130, 246, 0.08);
  color: #1e293b;
  transform: scale(1.02);
}

.dropdown-item i {
  font-size: 0.8125rem;
  width: 14px;
  transition: color 0.2s ease;
}

/* Primary Item (Voir) avec gradient */
.dropdown-item-primary {
  position: relative;
  overflow: hidden;
}

.dropdown-item-primary::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(45deg, #ff6b6b, #4ecdc4, #45b7d1, #96ceb4, #ffeaa7, #dda0dd, #98d8c8);
  background-size: 400% 400%;
  opacity: 0;
  transition: opacity 0.3s ease;
  animation: gradientShift 3s ease infinite;
}

.dropdown-item-primary:hover::before {
  opacity: 0.1;
}

.dropdown-item-primary:hover {
  color: #1e293b;
  background: transparent;
}

/* Danger Item (Supprimer) */
.dropdown-item-danger:hover {
  background: rgba(239, 68, 68, 0.08);
  color: #dc2626;
}

.dropdown-item-danger:hover i {
  color: #dc2626;
}

@keyframes gradientShift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

/* Actions du bas */
.bottom-actions {
  margin-top: 2rem;
  text-align: center;
}

/* Responsive design */
@media (max-width: 768px) {
  .analyses-container { padding: 2rem 1rem; }
  .analyses-title { font-size: 2rem; }
  .analyses-card { padding: 1.5rem; }
  
  .filters-grid {
    grid-template-columns: 1fr;
    gap: 1rem;
  }
  
  .table-container {
    border-radius: 8px;
  }
  
  .analyses-table th,
  .analyses-table td {
    padding: 0.75rem;
    font-size: 0.8125rem;
  }
  
  .language-badge, .type-badge, .score-badge {
    padding: 0.1875rem 0.5rem;
    font-size: 0.75rem;
  }
  
  .btn-view {
    padding: 0.375rem 0.75rem;
    font-size: 0.75rem;
  }
  
  .btn-view span {
    display: none;
  }
  
  .empty-state {
    padding: 3rem 1rem;
  }
  
  .empty-icon {
    font-size: 3rem;
  }
}

@media (max-width: 640px) {
  .analyses-table {
    font-size: 0.75rem;
  }
  
  /* Responsive intelligent: garde Titre | Score | Action */
  .analyses-table th:nth-child(2),
  .analyses-table td:nth-child(2),
  .analyses-table th:nth-child(3),
  .analyses-table td:nth-child(3),
  .analyses-table th:nth-child(5),
  .analyses-table td:nth-child(5) {
    display: none;
  }
}
</style>

<script>
function updateFilters(filterType, value) {
  const url = new URL(window.location);
  
  if (value) {
    url.searchParams.set(filterType, value);
  } else {
    url.searchParams.delete(filterType);
  }
  
  window.location.href = url.toString();
}

// Dropdown functionality - VERSION PROPRE
function initDropdowns() {
  const dropdowns = document.querySelectorAll('[data-dropdown]');
  
  dropdowns.forEach(dropdown => {
    const trigger = dropdown.querySelector('[data-dropdown-trigger]');
    
    // Nettoyer les anciens event listeners en clonant
    if (trigger) {
      const newTrigger = trigger.cloneNode(true);
      trigger.parentNode.replaceChild(newTrigger, trigger);
      
      // Ajouter le nouveau listener
      newTrigger.addEventListener('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        
        // Close other dropdowns
        dropdowns.forEach(d => {
          if (d !== dropdown) {
            d.classList.remove('open');
          }
        });
        
        // Toggle current dropdown
        dropdown.classList.toggle('open');
      });
    }
  });
  
  // Close dropdowns when clicking outside
  document.addEventListener('click', function(e) {
    if (!e.target.closest('[data-dropdown]')) {
      dropdowns.forEach(dropdown => {
        dropdown.classList.remove('open');
      });
    }
  });
}

document.addEventListener('DOMContentLoaded', initDropdowns);
document.addEventListener('turbo:load', initDropdowns);
</script>